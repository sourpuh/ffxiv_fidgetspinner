# Fidget Spinner

`/fspin left` to spin left `/fspin right` to spin right

`/fspin $direction $speed` to spin slower IE `/fspin left 0.1`. You cannot spin faster than 1.

move or `/fspin` to interrupt spinning
